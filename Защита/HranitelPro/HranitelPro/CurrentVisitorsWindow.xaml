<Window x:Class="HranitelPro.CurrentVisitorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="👥 Текущие посетители на территории" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="👥 Список лиц, находящихся на территории" FontSize="16" FontWeight="Bold" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <DataGrid x:Name="dgvVisitors" Grid.Row="1" AutoGenerateColumns="True" 
                  IsReadOnly="True" Margin="0,0,0,10"
                  AlternatingRowBackground="#F9F9F9" FontSize="13">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#34495E"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnSave" Content="💾 Сохранить список" Width="140" Height="35" Margin="5"
                    Background="#27AE60" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnSave_Click"/>
            <Button x:Name="btnRefresh" Content="🔄 Обновить" Width="100" Height="35" Margin="5"
                    Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnRefresh_Click"/>
            <Button x:Name="btnClose" Content="❌ Закрыть" Width="100" Height="35" Margin="5"
                    Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>