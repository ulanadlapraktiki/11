<Window x:Class="WpfApp1.SecurityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="🛡️ Охрана - Управление доступом" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="🛡️ Охрана - Управление доступом посетителей" FontSize="16" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="lblUser" Margin="20,0,0,0" Foreground="#BDC3C7" FontSize="12" VerticalAlignment="Center"/>
                <Button x:Name="btnLogout" Content="🚪 Выход" Margin="20,0,0,0" Background="#E74C3C" 
                        Foreground="White" BorderThickness="0" Cursor="Hand" Padding="10,5" Click="BtnLogout_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#F8FAFE" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Дата:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker x:Name="dpDate" Width="120" Margin="5" SelectedDateChanged="ApplyFilter"/>

                <TextBlock Text="Тип:" VerticalAlignment="Center" Margin="15,0,0,5"/>
                <ComboBox x:Name="cmbType" Width="100" Margin="5" SelectionChanged="ApplyFilter"/>

                <TextBlock Text="Подразделение:" VerticalAlignment="Center" Margin="15,0,0,5"/>
                <ComboBox x:Name="cmbDepartment" Width="150" Margin="5" SelectionChanged="ApplyFilter"/>

                <Button x:Name="btnRefresh" Content="🔄 Обновить" Width="100" Margin="15,0,0,0" 
                        Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnRefresh_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#F8FAFE" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="🔍 Поиск:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="txtSearch" Width="250" Margin="5" Padding="8,5" TextChanged="ApplySearch"/>
                <TextBlock Text="(ФИО или номер паспорта)" VerticalAlignment="Center" Foreground="#7F8C8D" FontSize="11"/>
                <Button x:Name="btnClearSearch" Content="✖ Очистить" Width="80" Margin="10,0,0,0" 
                        Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClearSearch_Click"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="dgvRequests" Grid.Row="3" AutoGenerateColumns="True" 
                  SelectionMode="Single" IsReadOnly="True" Margin="0,0,0,10"
                  AlternatingRowBackground="#F9F9F9" FontSize="13">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#34495E"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button x:Name="btnAccess" Content="🚪 Управление доступом" Width="200" Height="50" Margin="10,5"
                    Background="#27AE60" Foreground="White" FontSize="14" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="BtnAccess_Click"/>

            <Button x:Name="btnInfo" Content="ℹ️ Информация о заявке" Width="200" Height="50" Margin="10,5"
                    Background="#3498DB" Foreground="White" FontSize="14" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="BtnInfo_Click"/>
        </StackPanel>
    </Grid>
</Window>